schema: 1
story: '1.5'
gate: CONCERNS
status_reason: 'AC1/3/4/5 met; AC2/AC6 parciais (bucket enforça 512KB + MIME whitelist server-side, UI de upload deferida); AC7 WAIVED (padrão Epic 1). Shipped em PR #8 e validado retroativamente por Story 2.3 (public_professionals_v). Non-blocking para Epic 1 close.'
reviewer: 'Quinn'
updated: '2026-04-23T00:00:00Z'
top_issues:
  - id: 'REQ-001'
    severity: medium
    finding: 'AC2 parcial — campo "photo" do registration form não tem UI de upload implementada (Task 3.4 + Task 5.5 deferidos para Phase 2)'
    suggested_action: 'Tech debt: implementar UI de upload com browser-image-compression (target 500KB, max 1600px) + Server Action uploadProfessionalPhoto quando Story 1.7 E2E setup estiver em prep'
  - id: 'REQ-002'
    severity: low
    finding: 'AC6 (photo resize ≤ 500KB) — MITIGADO POR BANCO: bucket professional-photos tem file_size_limit=524288 + allowed_mime_types=[image/jpeg, image/png, image/webp]. Qualquer tentativa de upload direta via API respeitará o limit. UI client-side resize complementar não implementada.'
    suggested_action: 'Considerar REQ-001 + REQ-002 como item unificado. Server-side guard cobre o essencial; UI resize evita UX ruim mas não é gate de segurança.'
  - id: 'TEST-001'
    severity: medium
    finding: 'AC7 não atendido — E2E Playwright CRUD (apps/web/e2e/professional-crud.spec.ts) WAIVED explicitamente em 2026-04-23 change log (mesma regra 1.4/1.6/1.7 — depende de setup Playwright runner)'
    suggested_action: 'Pareado com 1.6-TEST-001 e 1.7-TEST-001: setup único de Playwright + axe-core cobre 4 stories simultaneamente (1.4, 1.5, 1.6, 1.7)'
  - id: 'TEST-002'
    severity: low
    finding: 'Actions em profissionais/actions.ts (createProfessional, updateProfessional, toggleActiveProfessional, softDeleteProfessional) sem Vitest coverage — gap consistente com 1.6'
    suggested_action: 'Adicionar testes de validation edge cases (slug regex, commission 0/100, specialties maxLength, TOCTOU de limit 1-20) no sprint P1 de hardening'
  - id: 'MNT-001'
    severity: low
    finding: 'Limit 1-20 profissionais via COUNT+INSERT não-atômico — TOCTOU teórico em alta concorrência permitiria transient 21+'
    suggested_action: 'MVP-scale aceitável (1-2 users por salão). Phase 2: considerar CHECK constraint via trigger BEFORE INSERT ou advisory lock durante o COUNT.'
  - id: 'DOC-001'
    severity: low
    finding: 'Task 1.3 (documentar bucket config em packages/db/README.md) explicitamente não feita'
    suggested_action: 'Low-priority tech debt — adicionar seção "Storage buckets" no README quando houver outros buckets documentando'
waiver: { active: false }
quality_checks:
  - check: code_review
    verdict: PASS
    notes: 'Zod schemas bem estruturadas (slug regex ^[a-z0-9][a-z0-9-]*[a-z0-9]$, commission 0-100, specialties maxLen 20); slug dedup com cap 20; path convention {salon_id}/{professional_id}/{filename}; 4 Server Actions consistentes com padrão 1.6; types.ts centraliza schema reutilizado em Story 2.1 (agenda) e 2.4 (slots)'
  - check: unit_tests
    verdict: CONCERNS
    notes: '27/27 Vitest pass (per Dev validation); apenas login/actions.test.ts e healthz/route.test.ts têm test files — gap consistente com 1.6/1.7'
  - check: acceptance_criteria
    verdict: CONCERNS
    notes: 'AC1 (lista), AC3 (edit+soft delete), AC4 (inactive filter — validado retroativamente por public_professionals_v), AC5 (limit 1-20 server-side) totalmente met. AC2 parcial (photo UI ausente). AC6 parcial (server-side ✅, client UI ❌). AC7 WAIVED.'
  - check: no_regressions
    verdict: PASS
    notes: 'typecheck/lint/build clean (per Dev); 27/27 tests pass; Story 2.3 construída em cima da 1.5 via public_professionals_v (migration 20260423000000) sem regressão; view valida AC4 retroativamente (WHERE is_active = TRUE AND deleted_at IS NULL)'
  - check: performance
    verdict: PASS
    notes: 'COUNT query O(1) + INSERT em MVP scale; slug dedup loop bounded por cap 20; sem N+1 queries detectadas; working_hours_jsonb permite índice GIN futuro se necessário'
  - check: security
    verdict: PASS
    notes: |
      Storage bucket professional-photos robusto:
      - public SELECT intencional (perfil público via /{salon}/{professional})
      - INSERT/UPDATE/DELETE via storage.foldername(name)[1]::UUID IN (SELECT current_user_salon_ids()) — Postgres array parsing protege path injection
      - allowed_mime_types: image/jpeg, image/png, image/webp (server-side)
      - file_size_limit: 524288 bytes (512KB hard cap server-side — enforces AC6 mesmo sem UI)
      RLS policies idempotentes (DROP IF EXISTS + CREATE). TOCTOU no limit 1-20 é low-severity em MVP scale.
  - check: documentation
    verdict: CONCERNS
    notes: 'Story file + commit + change log v1.1 (checkbox audit 2026-04-23) thorough; Task 1.3 (packages/db/README.md) explicitamente não documentada → DOC-001'
handoff:
  next_agent: '@po'
  next_command: '*close-story 1.5'
  rationale: 'CONCERNS verdict — 6 tech-debt items rastreados em docs/qa/backlog.md (REQ-001 parceado com REQ-002 para unificação; TEST-001 pareado com 1.6/1.7 TEST-001; TEST-002, MNT-001, DOC-001 independentes). Nenhum bloqueador. Epic 1 pronto para declarar Complete após close-story 1.5.'
  does_not_block: ['2.3', 'Epic 1 close']
  unlocks: 'Epic 1 (Foundation & Salon Onboarding) declarado Complete após *close-story 1.5'
