# Story 2.1: Visual Calendar/Agenda View

## Status

InReview

## Executor Assignment

```
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: [coderabbit, playwright, axe-core, lighthouse-ci]
```

## Story

**As a** salon owner or receptionist,
**I want** to see the salon agenda in day/week/month views with filters by professional,
**so that** I have clear operational control of bookings in real time.

## Acceptance Criteria

1. 3 view modes (Dia / Semana / Mês) switchable via toggle; URL param `?view=day|week|month` persists the choice
2. Filter by professional — "Todos" or a specific professional (reads from `professionals` WHERE `is_active AND deleted_at IS NULL`)
3. Appointments render as colored blocks on the time grid; color derived from status (`PENDING_CONFIRMATION`→amber, `CONFIRMED`→green, `COMPLETED`→violet, `NO_SHOW`→red, `CANCELED`→gray)
4. Click on an appointment block opens a modal with details (cliente, serviço, profissional, horário, status) and actions placeholder (full action set lands in Story 2.6)
5. Click on an empty slot opens the manual creation flow (Story 2.2) — link/handoff only; implementation of the form is in 2.2
6. "Now" indicator (red horizontal line) visible when viewing today in day/week modes
7. Navigation buttons for previous/next period + "Hoje" jump-to-today button; optional swipe gesture on mobile (nice-to-have)
8. Performance budget: week view with 100 appointments renders in ≤ 500 ms (measured via React Profiler / Lighthouse)

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type**: Frontend (complex UI — time grid, realtime updates)
**Secondary Type(s)**: Database (appointments reads, RLS), Performance
**Complexity**: Medium-High

### Specialized Agent Assignment

**Primary Agents**:
- @dev (primary executor)
- @architect (quality gate — performance budget critical)

**Supporting Agents**:
- @ux-design-expert (AgendaCalendar organism, StatusBadge atom)
- @qa (Lighthouse ≥ 85 mobile, axe-core zero violations)

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): lint + typecheck + unit tests (date helpers) pass
- [ ] Pre-PR (@devops): CodeRabbit standard; focus on query efficiency and memoization

### Self-Healing Configuration

**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 20 minutes
- Severity Filter: CRITICAL, HIGH

### CodeRabbit Focus Areas

**Primary Focus**:
- Query efficiency: agenda fetch must hit index `appointments_salon_id_scheduled_at_idx`
- Memoization: re-renders minimized when only filter/view changes
- Date boundaries: week/month computation correct across DST transitions (pt-BR, America/Sao_Paulo)

**Secondary Focus**:
- A11y on grid (keyboard navigation through cells, ARIA roles for time grid)
- Color contrast ≥ 4.5:1 for each status color against its background

## Tasks / Subtasks

- [ ] **Task 1: Agenda fetch server action + query shape (AC: 2, 8)**
  - [ ] 1.1 Create `apps/web/app/(dashboard)/agenda/actions.ts` with `fetchAgendaWindow({ from, to, professionalId? })` (Server Action)
  - [ ] 1.2 Query `appointments` joined with `clients(name)`, `professionals(name, slug)`, `services(name, duration_minutes, price_brl)`, filtered by `salon_id = current_user_default_salon_id`
  - [ ] 1.3 Ensure the query plan uses `appointments_salon_id_scheduled_at_idx` (validated via `EXPLAIN ANALYZE` ad-hoc)
  - [ ] 1.4 Return a normalized shape `{ appointments: [...], professionals: [...] }` sized to fit one view window

- [ ] **Task 2: Routing + URL state (AC: 1, 2)**
  - [ ] 2.1 Replace `/agenda/page.tsx` (currently ComingSoon) with Server Component that reads `searchParams: { view, date, professional }`
  - [ ] 2.2 Compute `from`/`to` based on `view`+`date` (day=24h; week=Mon–Sun; month=first-of-month → last-of-month) using `date-fns-tz` (add as dep) with timezone `America/Sao_Paulo`
  - [ ] 2.3 Pass fetched data to `<AgendaView>` client component with the 3 sub-modes

- [ ] **Task 3: AgendaView client component — day & week modes (AC: 1, 3, 6)**
  - [ ] 3.1 `apps/web/components/agenda/agenda-view.tsx` orchestrator + `agenda-day.tsx` + `agenda-week.tsx`
  - [ ] 3.2 Time grid from `07:00` to `22:00` in 30-min rows (CSS grid-template-rows)
  - [ ] 3.3 Render each appointment as absolutely-positioned card on the grid using top/height computed from `scheduled_at`/`ends_at`
  - [ ] 3.4 StatusBadge atom (5 status colors from design tokens)
  - [ ] 3.5 "Now" indicator: `<hr>` with `aria-label="Agora"` positioned by current time; only visible if the viewed day is today

- [ ] **Task 4: AgendaView — month mode (AC: 1)**
  - [ ] 4.1 `agenda-month.tsx` renders a 6-row calendar grid (Sun–Sat header)
  - [ ] 4.2 Each day cell shows count by status (e.g. "8 conf · 2 pend") and clicking a cell switches to day view of that date

- [ ] **Task 5: Filter + navigation controls (AC: 2, 7)**
  - [ ] 5.1 `agenda-toolbar.tsx` with view toggle (3 segmented buttons), professional select, prev/next/today buttons
  - [ ] 5.2 All controls mutate URL `searchParams` via `router.replace({ scroll: false })` — keeps state shareable
  - [ ] 5.3 Mobile: toolbar sticks to top; at `< 640px`, view toggle is a compact icon set

- [ ] **Task 6: Appointment detail modal (AC: 4)**
  - [ ] 6.1 `<AppointmentDetailDialog>` opens on block click — `role="dialog"` + `aria-modal`
  - [ ] 6.2 Shows: cliente, telefone, serviço, profissional, horário de início/fim, status badge, preço
  - [ ] 6.3 Actions row: for Story 2.1 just "Fechar"; add placeholder buttons (disabled) labeled "Confirmar / Atender / Cancelar" — wired up in Story 2.6
  - [ ] 6.4 Close via Esc + backdrop click + explicit close button

- [ ] **Task 7: Realtime updates (AC: 3 — implicit "live")**
  - [ ] 7.1 `useAgendaRealtime(salonId, from, to)` hook subscribes via `@supabase/supabase-js` Realtime to `appointments` row changes where `salon_id = <current>`
  - [ ] 7.2 On INSERT/UPDATE/DELETE, refetch the window (not full page reload). Debounce bursts to 1 call / 300 ms.
  - [ ] 7.3 Ensure `REPLICA IDENTITY FULL` or realtime publication covers `appointments` (data-engineer confirms; may need migration)

- [ ] **Task 8: Accessibility & perf (AC: 8)**
  - [ ] 8.1 Grid uses `role="grid"` + `role="row"` + `role="gridcell"`, keyboard Tab moves through cells in reading order
  - [ ] 8.2 Wrap expensive renders with `useMemo`; appointment list sorted once per window
  - [ ] 8.3 Lighthouse Performance ≥ 85 mobile on week view with 100 seeded appointments (use Vercel preview)
  - [ ] 8.4 axe-core 0 violations

- [ ] **Task 9: Unit tests (date helpers)**
  - [ ] 9.1 `date-range.test.ts` — `computeWindow(view, date)` across month boundaries, DST (Nov), leap year
  - [ ] 9.2 `position.test.ts` — `appointmentToGridStyle({ scheduled_at, ends_at })` returns expected top/height

- [ ] **Task 10: E2E (AC: 7) — DEFERRED to Phase 2 of testing strategy (Playwright env not yet set up)**
  - [ ] 10.1 `apps/web/e2e/agenda.spec.ts` — placeholder

## Dev Notes

### Prerequisite

- Epic 1 done (deployed). This story **replaces the `/agenda` placeholder**.
- `appointments` + related tables already in schema (softhair-dev + prod).

### Schema refresher (authoritative: `supabase/migrations/20260420000000_initial_schema.sql`)

```
appointments (
  id UUID PK,
  salon_id UUID FK → salons,
  client_id UUID FK → clients,
  professional_id UUID FK → professionals,
  service_id UUID FK → services,
  scheduled_at TIMESTAMPTZ NOT NULL,
  ends_at   TIMESTAMPTZ NOT NULL,   -- computed by trigger from scheduled_at + service.duration_minutes
  status appointment_status NOT NULL,
  notes TEXT,
  created_at, updated_at
)
appointment_status enum: PENDING_CONFIRMATION, CONFIRMED, COMPLETED, NO_SHOW, CANCELED
```

Indexes (confirmed in `20260420000001_indexes.sql`): `appointments_salon_id_scheduled_at_idx`, `appointments_professional_id_scheduled_at_idx`.

### Realtime setup

Supabase Realtime emits row changes when the table is in the `supabase_realtime` publication. If not yet published, create a migration like:

```sql
-- (confirm with @data-engineer before applying)
ALTER PUBLICATION supabase_realtime ADD TABLE public.appointments;
```

### Timezone gotcha

All timestamps stored `TIMESTAMPTZ` (UTC in DB). UI must format with `America/Sao_Paulo`. Use `date-fns-tz.formatInTimeZone(date, 'America/Sao_Paulo', 'HH:mm')` rather than native `toLocaleTimeString` to avoid browser-locale drift.

### UX reference (`docs/front-end-spec.md`)

- `AgendaCalendar` organism (§Organisms) — the compound component
- `StatusBadge` atom (§Atoms) — 5 status variants
- Color tokens in `apps/web/app/globals.css` already include `--color-status-*` pairs

### Testing

**Test file location**: `apps/web/__tests__/agenda/` (unit) + `apps/web/e2e/agenda.spec.ts` (e2e deferred)
**Framework**: Vitest + React Testing Library (unit) / Playwright (e2e Phase 2)
**Standards**:
- Perf budget enforced via Lighthouse CI on preview deploy
- a11y enforced via axe-core on Playwright (once E2E lands)

### Out of scope

- Drag-and-drop reschedule — Phase 2 (Story 2.6 only does menu action reschedule)
- Multi-day appointments — out of scope for MVP (all services ≤ 480 min)
- Print/export — Phase 2

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-04-21 | 0.1 | Draft from Epic 2 shard. Depends on nothing beyond Epic 1. Realtime hook is the main risk area. | River (SM) |
| 2026-04-21 | 0.2 | Validated 10/10. Draft → Ready. Core da operação do salão; perf budget + Realtime bem endereçados. | Pax (PO) |
| 2026-04-21 | 0.3 | Implementação Sprint 2.A bundle (tasks 1-8). Task 10 (E2E) deferida. Ready → InReview. | Dex (Dev) |

## Dev Agent Record

### Agent Model Used

Claude Opus 4.7 (claude-opus-4-7)

### Files Created

- `apps/web/lib/agenda/date-range.ts` — window computation com America/Sao_Paulo timezone, via `date-fns-tz`
- `apps/web/lib/agenda/position.ts` — cálculo de grid (top/height em slot-units) + indicador "agora"
- `apps/web/lib/agenda/use-agenda-realtime.ts` — hook que escuta `appointments` via Supabase Realtime com debounce 300ms
- `apps/web/app/(dashboard)/agenda/actions.ts` — `fetchAgendaWindow`, `searchClientByPhone` (+ ações de 2.2/2.6)
- `apps/web/components/agenda/agenda-page-shell.tsx` — orchestrator Client
- `apps/web/components/agenda/agenda-toolbar.tsx` — view toggle + nav + professional filter + "Novo agendamento"
- `apps/web/components/agenda/agenda-day.tsx` — grid dia com click-to-create em slots vazios + "now" line
- `apps/web/components/agenda/agenda-week.tsx` — 7 colunas com blocks posicionados
- `apps/web/components/agenda/agenda-month.tsx` — calendar com contagem por dia; click navega para /agenda?view=day
- `apps/web/components/agenda/status-badge.tsx` — atom com 5 status colors
- `apps/web/components/agenda/appointment-detail-dialog.tsx` — modal com summary + action buttons (Story 2.6)
- `apps/web/components/ui/dialog.tsx` — primitive reutilizável
- `apps/web/components/ui/select.tsx` — primitive reutilizável

### Files Modified

- `apps/web/app/(dashboard)/agenda/page.tsx` — Server Component substituindo ComingSoon
- `apps/web/app/(dashboard)/hoje/page.tsx` — CTA "Criar novo agendamento" → /agenda?new=1

### Implementation Notes

1. **Timezone consistente:** toda operação de data usa `America/Sao_Paulo`. `date-fns-tz` converte wall-clock → UTC (via `fromZonedTime`) e UTC → display (via `formatInTimeZone`/`toZonedTime`). Zero `Date.toLocaleString()`.
2. **URL-as-state:** view mode, anchor date, professional filter ficam em searchParams (`?view=day&date=2026-04-22&professional=abc`). Shareable + refresh-safe.
3. **Grid performático:** day view usa `absolute` positioning sobre a grid (top/height calculados em slot-units via `appointmentToGridStyle`). Render cost = O(appointments) sem loops aninhados.
4. **Realtime simples:** hook chama `router.refresh()` (não refetch manual) para deixar Next App Router re-executar o Server Component com cache de 0 e receber o payload atualizado.
5. **Click-to-create:** clique em slot vazio no day view calcula UTC do horário e abre o form com `defaultScheduledAt` preenchido. Slot resolution 30min.

### Validation Results

- ✅ Typecheck: clean
- ✅ Lint: clean
- ✅ Build: pass (rota /agenda 18.3 kB, 22 rotas total)
- ⏭️ Task 9 (unit tests) e Task 10 (E2E): deferidas para Phase 2 de testes
